sudo: required
language: generic 

services:
   - docker

before_install:
  - docker build -t tawfiq/docker-react -f
   Dockerfile.dev .
\script:
   - docker run -e CI=true tawfiq/docker-react npm run test
